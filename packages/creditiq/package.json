{
  "name": "@proworks-hub/creditiq",
  "version": "0.23.0",
  "type": "module",
  "description": "CreditIQ \u2014 credit exposure as a known floor under a per-class coverage manifest: a breach is assertible on partial evidence, compliance never is; mitigants with unevaluable conditions do not reduce; the decision regime is a required argument; and an adverse action is impossible without a decomposition that reconciles exactly to the score it explains.",
  "author": "Steven Kreutzer",
  "license": "UNLICENSED",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Proworks-Hub/proworks-engine-suite.git",
    "directory": "packages/creditiq"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "dependencies": {
    "@proworks-hub/contracts": "^0.23.0",
    "@proworks-hub/core-kit": "^0.23.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "@types/node": "^22.10.2"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -b",
    "prepare": "tsc -b"
  }
}
