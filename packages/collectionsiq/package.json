{
  "name": "@proworks-hub/collectionsiq",
  "version": "0.23.0",
  "type": "module",
  "description": "CollectionsIQ \u2014 collections that never dun a disputed amount, never assume an unknown call outcome in the collector's favour, never contact outside the contacted party's own quiet hours (unknown timezone refuses), evaluate every exit condition at every dunning step, cap computation at candidacy (a human authorizes), and report action-outcome association with causalClaim none and the confounders named.",
  "author": "Steven Kreutzer",
  "license": "UNLICENSED",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Proworks-Hub/proworks-engine-suite.git",
    "directory": "packages/collectionsiq"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "dependencies": {
    "@proworks-hub/contracts": "^0.23.0",
    "@proworks-hub/core-kit": "^0.23.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "@types/node": "^22.10.2"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -b",
    "prepare": "tsc -b"
  }
}
