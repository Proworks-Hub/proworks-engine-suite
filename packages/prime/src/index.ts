// Prime — the portable decision engine. Depends only on the DecisionContext,
// DecisionResult, and DecisionEngine contracts, and only at the type level,
// so it carries no runtime dependency on ForgeIQ or CostIQ.
export * from "./primeEngine";
